import React from "react";
import { Link } from 'react-router-dom';
const date = (value) => value ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium' }).format(new Date(value)) : '—';
export default function ItemCard({ item, onStatus, onDelete }) { const target = item.status === 'lost' ? 'found' : 'lost'; return <article className="item-card"><div className="item-card-main">{item.photoUrl ? <img src={item.photoUrl} alt="" /> : <div className="item-placeholder" aria-hidden>◆</div>}<div><div className="item-title"><h3>{item.name}</h3><span className={`badge ${item.status}`}>{item.status}</span></div><p>{item.category || 'Uncategorized'} · {item.scanCount} scans</p><small>Created {date(item.createdAt)} · {item.qrToken}</small></div></div><div className="card-actions"><Link className="button secondary small" to={`/dashboard/items/${item.id}`}>View</Link><button className="button secondary small" onClick={() => onStatus(item, target)}>Mark {target}</button><button className="text-danger" onClick={() => onDelete(item)}>Delete</button></div></article>; }
